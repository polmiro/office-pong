/** @jsx React.DOM */

var TournamentPlayer = React.createClass({
  propTypes: {
    player: React.PropTypes.object.isRequired,
    scores: React.PropTypes.array.isRequired
  },

  render: function() {
    return (
      <div>
      </div>
    );
  }
});