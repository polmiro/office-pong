/** @jsx React.DOM */

var Tournament = React.createClass({
  propTypes: {
    model: React.PropTypes.object
  },

  render: function() {
    return (
      <div>
        Tournament
      </div>
    );
  }
});