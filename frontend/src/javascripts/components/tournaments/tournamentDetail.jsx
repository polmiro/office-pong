/** @jsx React.DOM */

var TournamentDetail = React.createClass({
  propTypes: {
    model: React.PropTypes.object.isRequired
  },

  render: function() {
    return (
      <div>
        This is the Tournament
      </div>
    );
  }
});